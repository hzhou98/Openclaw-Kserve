# =============================================================================
# OpenClaw Helm Values — OpenAI API only (no local GPU model)
# =============================================================================
#
# Use this instead of values.yaml when you don't have a GPU node or the
# KServe InferenceService isn't ready. This connects OpenClaw directly to
# OpenAI's API — no KServe, no vLLM, no GPU required.
#
# Prerequisites:
#   - Set OPENAI_API_KEY before running install-openclaw.sh:
#       export OPENAI_API_KEY=sk-...
#       bash openclaw/install-openclaw.sh --values values-openai.yaml
#
# The default model is gpt-4o-mini (cheapest). Change
# agents.defaults.model.primary to use a different model.
# =============================================================================
app-template:
  controllers:
    main:
      containers:
        main:
          envFrom:
            - secretRef:
                name: openclaw-env-secret

  configMaps:
    config:
      enabled: true
      data:
        openclaw.json: |
          {
            "gateway": {
              "port": 18789,
              "mode": "local",
              "auth": {
                "mode": "token",
                "token": "${OPENCLAW_GATEWAY_TOKEN}"
              },
              "controlUi": {
                "dangerouslyAllowHostHeaderOriginFallback": true
              }
            },
            "models": {
              "mode": "merge",
              "providers": {
                "openai": {
                  "baseUrl": "https://api.openai.com/v1",
                  "api": "openai-responses",
                  "apiKey": "${OPENAI_API_KEY}",
                  "models": [
                    {
                      "id": "gpt-4o",
                      "name": "GPT-4o",
                      "reasoning": false,
                      "input": ["text", "image"],
                      "contextWindow": 128000,
                      "maxTokens": 16384
                    },
                    {
                      "id": "gpt-4o-mini",
                      "name": "GPT-4o Mini",
                      "reasoning": false,
                      "input": ["text", "image"],
                      "contextWindow": 128000,
                      "maxTokens": 16384
                    }
                  ]
                }
              }
            },
            "agents": {
              "defaults": {
                "model": {
                  "primary": "openai/gpt-4o-mini"
                }
              }
            }
          }

  persistence:
    data:
      enabled: true
      size: 5Gi
      accessMode: ReadWriteOnce
